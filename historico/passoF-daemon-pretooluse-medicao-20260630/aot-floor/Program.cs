// ptu-client-floor - PROTOTIPO DESCARTAVEL (floor de spawn AOT). Sai imediatamente.
// Passo F: mesmo floor do passo 0 (return 0;). Isola spawn+runtime+teardown de um processo
// NativeAOT win-x64 -- o "piso" sobre o qual o overhead do daemon e' medido (e2e - floor).
return 0;
