# Ideias Implementadas

Registro de ideias que sairam de `999-ideias-pendentes.md` por terem sido implementadas ou incorporadas ao contrato metodologico vigente.

## Procedure.Link em parametros de action no KbIntelligence

Implementado em 2026-07-31 para fechar falso negativo real observado em `FabricaBrasil`: actions `WorkWithForWeb` chamavam a procedure wrapper `procDisplayUrlSemPaginaEmbutida` por `gxobject`, mas passavam a procedure de relatório real como primeiro parâmetro em `Procedure.Link(...)`; o `who-uses` enxergava o wrapper e perdia a dependência indireta.

O extrator `scripts/Build-KbIntelligenceIndex.py` passou a reconhecer `Procedure.Link(...)` em `Source` efetivo, `Property Formula` e parâmetros de action `WorkWithForWeb`, emitindo `procedure_dot_link`, `attribute_formula_procedure_dot_link` e `workwith_action_parameter_procedure_dot_link`. A relação `workwith_action_gxobject` com o wrapper foi preservada. `EXTRACTOR_SIGNATURE_VERSION` mudou de `10` para `11`, então índices existentes precisam de rebuild para expor a nova cobertura.

Validação: `scripts/Test-KbIntelligenceProcedureCallFormsSelfTest.ps1` cobre `Procedure.Link(...)` em `Source`, `WebPanel` com `Extensions.Web.Window.OpenNewWindow(...)`, fórmula de atributo e action com wrapper. Em índice temporário contra `C:\Dev\Prod\Gx_FabricaBrasil\ObjetosDaKbEmXml`, `who-uses` passou a retornar os usos indiretos de `procRelatorioPedidosDeEmbarque` e `procRelatorioAutorizacaoExpedicao` em `WorkWithWebVendaPedido`, além dos casos correlatos em `WorkWithWebCarga`, `wpCarga` e `wpCargaPainel`.

## Follow-up de cobertura da janela pós-build do PR #2

Implementado na PR #2 `fix: refine build post-processing classification`: o self-test cobre uma janela `Executando eventos pós-construção` detectada, contendo apenas saída inerte de duração/data, com um `start c:\temp\fora-da-janela.bat` antes do marcador. A extração retorna zero eventos e não executa o fallback global quando a janela existe.

O caso evita **falso rebaixamento** por um comando externo à janela; não é um caso de falso sucesso limpo. A paridade de `Invoke-GeneXusKbSpecifyGenerate.ps1` e a evidência empírica .NET Framework permanecem como follow-ups vivos na entrada do contrato v2/finalizador compartilhado.

## Criar/alterar objeto GeneXus do tipo `API` (from-spec, com segurança GAM) nas skills XPZ

- **Importância** — média (gap real com workaround manual; risco de **falso-positivo de segurança**). Hoje um agente em pasta paralela tem o envelope/clonagem coberto, mas nada que o guie em "API + **GAM** + **validação de runtime**" — a parte que mais deu trabalho no caso real. Sem isso, um objeto `API` com `[SecurityLevel(Authorization)]` pode ser entregue como "seguro" **sem enforcement** (aplicação GAM com "Habilitar autorização?" desmarcada) — falso-positivo que parece seguro e não é.
- **Maturidade** — pesquisa feita (design **congelado** por revisão por pares — v1→v3, 3 famílias: anthropic/openai/nvidia; recibo ao fim). Decisões de design fechadas; o **gatilho de implementação é a Fase 0 empírica** numa KB de teste, que pode refinar detalhes de molde/contrato.

**Definição de "from-spec":** o agente escreve/edita o objeto `API` em XML/XPZ (partindo de molde sanitizado mínimo) + SDTs + Procedures — **não** clona cadeia real de KB, **não** parte de OpenAPI nem da DSL da nexa. Distinto da **clonagem** que o `03` fechou (arrasta a subárvore de 3904 objetos do `XPZExemploCadeiaAPIA.xpz`).

**Enquadramento (espinha congelada) — duas frentes:**

- **Frente A — Construção from-spec:** objeto `API` + contrato de dados (SDT request/response) + Procedures de implementação. O risco real está na subárvore (`03:286`), não no envelope.
- **Frente B — Segurança GAM, 3 faces:**
  - **Face 1 (atributo no objeto):** `[SecurityLevel]`/`[SecurityPermission]` — mora na `nexa`; **referenciar, não duplicar**. Exceção XPZ estreita: o literal de `[SecurityPermission]`, **quando presente**, deve casar com a permissão `*_Services_*` gerada (checável estaticamente; a `ProdutoApi` real tinha `[SecurityLevel(Authorization)]` **sem** `[SecurityPermission]`, logo a checagem é condicional à annotation existir).
  - **Face 2 (config runtime GAM):** "Habilitar autorização?", papel, permissão, vincular usuário, aplicação certa (não Backoffice) — **fora do escopo XPZ como automação**; vira **pré-condição operacional documentada** num satélite novo `api-gam-runtime.md` **ancorado pelo `api.md`**, que **bloqueia conclusão indevida**. Fora do escopo confirmado por `nexa/SKILL.md:26-27` (exclui admin de runtime/DB).
  - **Face 3 (prova de runtime):** smoke 2 fases (401/403/200) + OAuth + body + multi-environment — **checklist textual + sub-estado pós-build** (reusar `operação concluída, pendente de confirmação funcional` do `xpz-msbuild-build/SKILL.md:371,481`); gate scriptado = evolução futura (não cabe como `9-*`, que é preflight estático pré-import).
  - Notas: 4º modo (autorização via evento, `If NOT IsRegisteredUser → 403`, **fora** do caminho canônico GAM); OpenAPI `oAuthGXGAM` **≠** enforcement.

**Plano em 3 fases:**

- **Fase 0 (empírica, pré-requisito — gatilho de implementação):** numa KB de teste, em ordem: **(0a)** API montada **from-spec importa+builda+expõe REST** headless (separar do que `03:283` já provou p/ clonagem: lá o envelope passou do parse); **(0c, antes do molde)** validar string `Authorize` vs `Authorization` no **parser** (D3 — senão o molde nasce inválido); **(0b)** derivar **molde mínimo autocontido** — caminho **primário: reduzir `01e:598`** (já sanitizado, no repo); **validação cruzada:** sanitizar a `ProdutoApi` real (checklist abaixo); definir o que é **"autocontido"** (quais deps aceitáveis, ex.: `sdt:Messages, GeneXus.Common` sim? `exo:GAMSession` não?); **(0d)** reproduzir mapeamento C# (`SecurityNone/Low/High`) **datando versão GeneXus/GAM**; **(0e)** smoke 2 fases + OAuth + body (envelopado por nome de variável vs tipo do SDT root) + `PUT`. **Levantar a cadeia de gates** que o batch API+SDT+Procedure dispara: **9-IDO** (ordenação; aborta só em **ciclo**, cadeia linear é `alert`/staging), **9-BC** (Procedure com `bc:`), **9-PNW** (CRUD insert tem `New`), 9-TWS/9-TXW (se houver Transaction); verificar se `Test-GeneXusBatchDependencyOrdering.ps1` cobre a cadeia.
- **Fase A (documental):** **A1** elevar `api.md` (ponte à nexa + nota OpenAPI≠evidência + nota 4º modo + nota `[SecurityPermission]`↔`*_Services_*` condicional + ancorar o satélite); **A2 enriquecer moldes** (`01e`) com o molde mínimo (preferência: reduzir `01e:598`; `ProdutoApi` sanitizada como 2º caso/validação, com **contrato completo**: request `ProdutoId/ProdutoNome`, response `Found/Code/Message` — não "só 2 atributos"); **A3** recalibrar `03` (enquadramento "API-cadeia-grande vs API-autocontida" **e atualizar a tabela de risco**, não só reenquadrar); **A4 estender** (não "afiar") `02`/`08`/**`10-base-operacional-msbuild-headless.md`** com a dimensão GAM/runtime + README trilíngue.
- **Fase B (o vão, depende de A + F0):** **B1** satélite `api-gam-runtime.md` (Face 2, pré-condição que bloqueia conclusão indevida); **B2** critério de aceite (smoke 2 fases + **multi-environment com escopo explícito** — quais ambientes agora, quais pendência + OAuth datado + body + resíduo de smoke; reusar sub-estado existente); **B3** contrato (SDT request/response + Procedures de implementação + ordem de import + cadeia de gates).

**Decisões fechadas:** D1 (Face 2 fora de automação, satélite ancorado); D2 (Face 3 checklist+sub-estado, não gate `9-*`); "from-spec" = agente edita XML; molde = reduzir `01e:598` (primário) + `ProdutoApi` (validação).

**Validações empíricas abertas (resolver na Fase 0, NÃO em painel):** D3 (`Authorize`/`Authorization` no parser); "autocontido" (fronteira de deps); a cadeia de gates cobre API+SDT+Procedure?; from-spec importa+builda?; mapeamento C# por versão; body envelope (nome de variável vs tipo); `PUT 404` é local de IIS/environment?

**Checklist de sanitização do molde de API (a especificar — gap de implementação):** neutralizar `guid`/`parentGuid`/`moduleGuid`/`type`/`parentType`, `checksum`, `user` (padrão `SANITIZED\USER`), nomes de empresa/IDs reais, caminhos/URLs de servidor. **O objeto bruto da pasta paralela nunca toca o repo — só a versão sanitizada.** Análogo à sanitização já usada nos moldes de Transaction/Procedure.

**Riscos de generalização (datar como empírico, versão GeneXus/GAM):** mapeamento C#, códigos OAuth (542/116/232/79), body envelopado, `PUT→404` (quirk de IIS/environment — a nexa usa `PUT` normal no Exemplo 2), nomes `*_Services_*`. O **falso-positivo de autorização** generaliza (design do GAM), mas documentado como **pré-condição verificável**, não como "falso-positivo é esperado".

**Paridade ao implementar:** `api.md` (+ satélite novo `api-gam-runtime.md`), `02`, `08`, `10-base-operacional-msbuild-headless.md`, `03` (texto + tabela de risco), README trilíngue, e — se nascer script — `09`/`CHANGELOG`.

**Origem e recibo da revisão por pares (congelamento):** frente nascida do trabalho real de um agente Codex que criou/validou a `ProdutoApi` (CRUD com GAM) na KB de teste `Gx_wsEducacaoSpTeste` (dois `.md` de aprendizado/handoff na pasta paralela, fora do repo). Design **congelado** em 2026-06-28 após 3 rodadas de revisão por pares (v1→v3): v1 = 3 famílias (anthropic/openai/nvidia, 4 pareceres); v2 = 2 famílias (anthropic/openai; nvidia falhou por truncamento/timeout); v3 = 3 famílias (anthropic nativo, openai/Codex gpt-5.5, nvidia/kimi-k2.6). Convergência: a **arquitetura não foi reaberta em 3 versões**; objeções remanescentes só de **precisão de implementação** → decisão humana de **congelar o papel e transferir a prova para implementação/self-test** (`15-revisao-por-pares.md`, "Quando o design estabiliza"). Vozes que falharam por **infra** (registrar, não confundir com qualidade): ollama-cloud (cota semanal 429), siliconflow (saldo insuficiente), nvidia deepseek-v4-pro/glm-5.1 (timeout 280s sob concorrência), OpenCode Zen grátis deepseek-v4-flash/mimo-v2.5/north-mini-code (truncam por `tool-calls` / raquítico — **reprovados** como revisores para manuscrito denso). Nemotron 3 Ultra **não** usado (veto duro).

### Resultados da Fase 0 empírica — EXECUTADA 2026-07-01 (KMW 4.0.187794 + GAM 3.15.78, KB Gx_wsEducacaoSpTeste)

Pacote empírico rodado após revisão por pares (5 vozes/3 famílias: anthropic nativo, openai/gpt-5.5, ollama-cloud deepseek-v4-pro/kimi-k2.7-code/glm-5.2) que reviu H1/H2/H3. **Achado transversal: preview ≠ validação real** — o preview de import dá `importTaskSuccess:true` para API inválida; só o **import real** roda a validação de gramática/referências da API. Logo "0a importa" exige **import real**, nunca preview.

- **D3 RESOLVIDO (parser):** `[SecurityLevel(Authorize)]` é **REJEITADO** — `mismatched input 'Authorize' expecting {ModeNone, ModeAuthorization, ModeAuthentication}`. Válidos: `None`/`Authentication`/`Authorization`. O molde usa **`Authorization`**. A referência **nexa `object-api.md:159` está factualmente errada** (lista `Authorize`); a página da propriedade (`properties-common-integrated-security.md:14`) e a evidência real usam `Authorization`. Nota no `api.md` **por observação**, não hipótese.
- **Mapeamento C# (0d) datado:** `None→SecurityNone`, `Authentication→SecurityLow`, `Authorization→SecurityHigh` @ KMW 4.0.187794 + GAM 3.15.78.
- **Cadeia de gates / H2 FECHADO:** o import real valida a referência `API => Proc`. Rejeita se o Procedure está **ausente do lote E da KB** (`Object Reference ... not found`); **resolve intra-lote** quando API+Proc estão no mesmo pacote (testado: import OK dos 2); e resolve contra a KB pré-existente (staging). A hipótese "`API=>Proc` resolvido no build" (deepseek) está **empiricamente errada** — é resolvido no **import real**. A `Test-GeneXusBatchDependencyOrdering.ps1` (9-IDO) **não modela** `API→Proc` (só arestas de Procedure), mas isso é **doc-gap**, não script-gap: o próprio import resolve intra-lote/contra-KB. **Regra a documentar:** empacotar a API junto com seu Procedure, ou fazer staging do Procedure antes da API — **nunca** importar uma API cujo Procedure de implementação esteja ausente de ambos.
- **Enforcement reversível PROVADO (item 1, nova evidência desta sessão):** app no ar em `http://localhost/wsEducacaoSpTesteNETFrameworkSQLServer`; anônimo→**401**; usuário com papel `ProdutoApiConsumidor`→**200**; **papel revogado→403** (`code 139, "Não autorizado: acesso negado."`). Fecha o falso-positivo de segurança (o P0 da voz nativa): o GAM checa autorização por requisição contra as permissões atuais.
- **Gaps do painel a incorporar nas fases documentais A/B:** body **envelopado pelo nome da variável** como contrato explícito de consumo; `PUT→404` como limitação de ambiente + fallback `POST .../alteracao` (nunca como contrato de plataforma); carimbo de versão (KMW+GAM) em cada fato datável; códigos OAuth 542/116/232/79 em seção "erros conhecidos"; **"autocontido" (0b) RESOLVIDO** — molde mínimo = tríade `API`→`Procedure`→`SDT`; única `ATTCUSTOMTYPE` externa = `sdt:` próprios; exclui `exo:GAMSession, GeneXusSecurity` (4º modo) e `sdt:Messages, GeneXus.Common` (opcional); tríade sanitizada gravada em `01e` (A2); escopo da evidência (classe "API from-spec sobe" ≠ molde específico).
- **Resíduos:** `zzApiBatchTest`+`procFase0BatchDep` importados na KB de teste (a remover pela IDE); papel `ProdutoApiConsumidor` revogado do `api_produto_teste` (a restaurar); artefatos de trabalho da pasta paralela apagados. Divergência à parte: `kb_environment_web_dirs` registra `NETFrameworkSQLServer/web`, real é `NETFrameworkSQLServer004/web`.

**Status final (2026-07-01): frente documental IMPLEMENTADA e encerrada.** Fase 0 empírica fechada; A1 (`api.md` elevado), A2 (`01e` tríade), B1 (`api-gam-runtime.md`) no commit `7dc1bd3`. **A3** — recalibração do `03` distinguindo `API (cadeia)` de `API (from-spec)` em texto e nas 2 tabelas de risco (micro-notas de recalibração, `medio-contextual`, célula de `parent` corrigida, remissões aos satélites), via **revisão por pares** (6 versões, 3 famílias: anthropic nativo, openai/gpt-5.5, ollama-cloud deepseek/kimi/glm) que pegou e corrigiu um erro factual (afirmar que o from-spec não tinha `parent` nomeado — o molde `01e` tem `parent="PastaExemploApi"`). **A4** — `02` (Politica para API + dimensão GAM/runtime), `08` (`### API`), `10-base` (achado empírico preview≠import real); README avaliado = sem edição (índice de alto nível). **B** — `xpz-builder/SKILL.md:52` expõe `api-gam-runtime.md` na enumeração de satélites. Pré-push reforçada (13/14) com 2 famílias (openai/Codex + anthropic nativo) + 1 lap ollama (deepseek) corrigiu gaps de paridade (CHANGELOG trilíngue, exceção API no `xpz-msbuild-import-export/SKILL.md`, notas stale em `01a`/`02`/`07`). **Item derivado deixado em aberto:** gate scriptado da Face 3 (smoke de runtime automatizado) — mini-entrada nova no `999-ideias-pendentes.md`, evolução futura (não cabe como `9-*`).

**Emenda incremental (2026-07-23, OnlineShopSS):** a frente recebeu evidência adicional em uma KB local de teste sem reabrir a arquitetura API+GAM de 2026-07-01. A prova OnlineShopSS confirmou a variante de listagem funcional `API -> Procedure -> SDT`: endpoint `GET /categories`, variável coleção da API e da Procedure com `AttCollection=True`, SDT de item com `AttCollection=False`, Procedure com `For each Category` sobre Transaction/base table real, `Category` coexistindo com `idISBUSINESSCOMPONENT=True` e `idIsDynTrn=False`, build/sync full limpos e HTTP 200 com payload JSON no ambiente servido. A emenda documenta apenas o delta líquido: `For each` não é chamado de "via BC" sem variável `bc:`, `idIsDynTrn=False` não vira receita universal de inicialização/carga e HTTP 200 não prova enforcement GAM. Referência de trabalho: `Temp\handoff-api-object-20260723.md`; o gate scriptado de runtime permanece como item futuro em `999`.

**Emenda incremental (2026-07-24, OpenAPI YAML como evidência complementar):** atualização **doc-only, sem script, sem KB real alterada**. A frente documenta que YAML OpenAPI já gerado pelo build/deploy pode complementar a evidência do contrato público observado de um API Object, desde que arquivo, ambiente e frescor sejam declarados. O YAML segue fora do papel de fonte de geração e fora da prova de segurança: presença ou ausência de `securitySchemes` não prova enforcement GAM nem ausência de configuração/autorização interna. A mudança não reabre `SpecifyOpenAPI`/`GenerateOpenAPI` como automação headless; apenas registra leitura de artefato já gerado quando disponível. Em pré-push reforçada, a rastreabilidade foi completada com `xpz-msbuild-build/SKILL.md`, porque a skill também orienta validação pós-build/deploy de endpoint `API`.

### Rastreabilidade

- Arquivos materiais: `03-risco-e-decisao-por-tipo.md`, `02-regras-operacionais-e-runtime.md`, `08-guia-para-agente-gpt.md`, `10-base-operacional-msbuild-headless.md`, `01a-catalogo-e-padroes-empiricos.md`, `07-open-points-e-checklist.md`, `01e-moldes-sanitizados-core.md`, `xpz-builder/SKILL.md`, `xpz-builder/responsibilities-by-type/api.md` (novo/elevado), `xpz-builder/responsibilities-by-type/api-gam-runtime.md` (novo), `xpz-msbuild-import-export/SKILL.md`, `xpz-msbuild-build/SKILL.md`, `CHANGELOG.md` (entrada trilíngue), `998-ideias-descartadas-e-porque.md` (ressalva OpenAPI YAML já gerado), `999-ideias-pendentes.md` (entrada removida → esta; item futuro do gate scriptado mantido como mini-entrada nova).
- Commits materiais: `7dc1bd3` (Fase 0 + A1/A2/B1), `133886b` (reconcilia status 0b), `d758f6c` (A3/A4/B), `9df971f` (paridade pós-pré-push).
- Revisão por pares: A3 = 6 versões / 3 famílias (anthropic nativo, openai/gpt-5.5, ollama-cloud deepseek-v4-pro/kimi-k2.7-code/glm-5.2); design congelado por decisão humana em 2026-06-28; pré-push reforçada com 2+3 famílias.
- Fase 0 empírica: KMW 4.0.187794 + GAM 3.15.78, KB de teste `Gx_wsEducacaoSpTeste`.

## Least-privilege do revisor opencode — «sem execução/escrita» (D-min)

Reúne as duas entradas do `999` que eram **problema** e **conserto** da mesma classe de risco: «Painel de revisão por pares concede execução de código a modelo externo em headless» e «Agente reviewer do opencode sem execução/escrita». Ambas **implementadas** e removidas do `999`.

- **Importância** — **ALTA / segurança-least-privilege**. Risco ativo, já **materializado** (2026-06-24: revisor opencode `kimi-k2.7-code`, agente default, **editou** um `.md` do repo em vez de só reportar). Em `opencode run` headless o agente default `build`/`plan` **auto-aprova** `bash`/`edit`/rede; em payload `public` o gate de confidencialidade libera automaticamente, então o risco de execução/escrita **não passava por nó humano**.
- **Escopo D-min (fechado):** fecha execução/escrita (`bash`/`edit`) e as **ferramentas** de rede (`webfetch`/`websearch`/`task`); confina a **leitura** ao workspace do cwd. **Não** fecha o canal do próprio parecer ao provider (residual aceito em `public`).

**Solução (design congelado `opencode-reviewer-ro-least-privilege-design.md`, 8 rodadas / 4 famílias):**

- **D1+D2 (adapter):** `Invoke-OpenCode.ps1`/`Start-OpenCodeJob.ps1` ganham **default `-Agent reviewer-ro`** escopado ao caminho revisor + **guard fail-closed** (pré-check estático + `opencode agent list` allow-set resolvido EXATAMENTE `{read,grep,glob,list}` + versão testada; BLOCK com motivo `static`/`version`/`allowset`/`agentlist`-transitório). `-Agent <x>` explícito = opt-out consciente (confirma só que resolve). Pós-check síncrono varre o warning de fallback; assíncrono é diagnóstico no `Watch-OpenCodeJob.ps1`. O painel segue bloqueando a chave `agent` → o revisor sempre cai no `reviewer-ro`. **Breaking change** do default (CHANGELOG).
- **D3 (provisionamento):** `.opencode/agent/reviewer-ro.md` (project-local versionado, forma `permission` `"*": deny` + allowlist) + `scripts/Install-OpenCodeReviewerRoAgent.ps1` (global, edição JSONC localizada preservando comentários, migra o interino `tools:`→`permission`) + `scripts/OpenCodeReviewerRoGuard.ps1` (guard compartilhado) + `scripts/Test-OpenCodeReviewerRoSelfTest.ps1` (`OPENCODE_REVIEWER_RO_SELFTEST_OK`) + fixtures versionados.

**Correção de premissas (medido inicialmente em opencode 1.4.4; fixtures promovidos para **1.17.20** —
o self-test é o gate dos claims):**

- **`permission: deny` ≡ `tools: false`** — a nota antiga do `999` («`tools: false` desabilita a ferramenta, **mais forte** que `permission: deny`») está **REFUTADA pela medição**: os dois resolvem idêntico e removem a tool em headless. A forma canônica é `permission` (curinga `"*": deny` funciona por last-match-wins; `webfetch`/`websearch`/`task: deny` removem as tools).
- **Eixo de leitura NÃO é machine-wide** — a nota antiga («`read` lê qualquer arquivo da máquina») está **invertida pela medição**: o opencode tem a dimensão nativa `external_directory` (base `ask`, auto-rejeitada em headless) que gateia leituras **fora** do cwd; o reviewer-ro fixa `external_directory: deny` explícito → leitura fora do cwd fica **bloqueada por padrão**, sem provar isolamento absoluto de todo path externo específico.

**ADIADO (entrada nova no `999`, eixo de leitura):** liberar opencode em `kb-sensitive`/pasta paralela (segue `unavailable`) + **mecanizar cwd-seguro** (hoje o bloqueio de leitura fora do cwd herdado não protege `.env`, logs, caches ou outros segredos não-versionados dentro do próprio cwd; escolher um cwd seguro é responsabilidade operacional de quem dispara).

### Rastreabilidade

- Design congelado: `opencode-reviewer-ro-least-privilege-design.md` (8 rodadas de revisão por pares, 4 famílias — anthropic/openai/ollama-cloud/nvidia; arquitetura nunca reaberta; freeze por decisão humana com a prova transferida aos self-tests).
- Arquivos materiais: `.opencode/agent/reviewer-ro.md`, `.gitignore` (exceção em cascata), `scripts/OpenCodeReviewerRoGuard.ps1`, `scripts/Install-OpenCodeReviewerRoAgent.ps1`, `scripts/Test-OpenCodeReviewerRoSelfTest.ps1`, `scripts/Invoke-OpenCode.ps1`, `scripts/Start-OpenCodeJob.ps1`, `scripts/Watch-OpenCodeJob.ps1`, `xpz-llm-delegate/fixtures/opencode-reviewer-ro/*`, `xpz-llm-delegate/SKILL.md`, `15-revisao-por-pares.md`, `xpz-skills-setup/SKILL.md`, `08-guia-para-agente-gpt.md`, `09-inventario-e-rastreabilidade-publica.md`, `CHANGELOG.md`, `999-ideias-pendentes.md` (2 entradas removidas → esta; entrada nova do eixo de leitura mantida).
- Self-test/versão: `OPENCODE_REVIEWER_RO_SELFTEST_OK`; fixtures/claims ancorados em opencode **1.17.20**
  (promovidos desde a implementação inicial em 1.4.4).

## Capturar a recusa real de workspace não confiável do Claude Code

- **Importância** — baixa-média na entrada original (detector heurístico podendo divergir da mensagem real da CLI). Na prática revelou-se **alta**: o detector não só divergia como **mascarava** todas as falhas do backend.
- **Maturidade** — era "ideia pronta para validar quando houver nova ocorrência". A ocorrência veio em 2026-07-25 e a captura foi feita.

**Origem da pendência:** revisão pré-push reforçada de 2026-07-15, após relato — sem `stderr` bruto — de que o Claude Code teria recusado executar em workspace não confiável. A entrada pedia preservar a evidência (stderr sem segredos, `claude --version`, contexto Desktop/CLI) para ancorar ou substituir a heurística.

**O que a captura mostrou.** A premissa estava errada nos dois eixos. A CLI **não recusa** executar em workspace não confiável: ela roda normalmente e apenas **descarta** as regras `permissions.allow` do projeto, avisando em `stderr`. E esse aviso aparece **inclusive em execução bem-sucedida** — medido com `exit 0`, resposta válida e o aviso presente byte a byte igual ao de uma execução falha. Em pasta sem `.claude/settings.json` para descartar, o aviso nem é emitido, ainda que o workspace continue não confiável.

Consequência: o detector `workspace-not-trusted` disparava a partir de um texto sem valor diagnóstico algum, e como ele era testado **antes** do filtro genérico de erro, sequestrava a classificação de qualquer falha — inclusive as que traziam a causa escrita em texto claro no `stdout`.

**Correções implementadas** (2026-07-25):

- ruído de ambiente (`Ignoring N permissions.allow entries…`, `Permission allow rule (…)`) descartado antes de qualquer classificação, em `Get-ClaudeCodeErrorMessage` e dentro do próprio `Test-ClaudeCodeWorkspaceNotTrusted`;
- código canônico novo `max-turns-exhausted` para esgotamento de turno, testado antes do detector de confiança;
- `workspace-not-trusted` preservado **apenas** para recusa genuína, documentado como heurístico e **nunca observado empiricamente**;
- fixtures de regressão ancoradas no `stderr` literal capturado, cobrindo o caso de sucesso e o de falha.

**Achado derivado da revisão pré-push (mesma sessão).** O caminho **assíncrono** não normalizava nada: `Watch-ClaudeCodeJob.ps1` só marcava erro em um evento `type=error`, que a CLI **não** emite no desfecho — medido com `claude 2.1.220`, a última linha do `--output-format stream-json` é `type=result` com `subtype` (`success`, `error_max_turns`, …) e `is_error` —, e `Resolve-ClaudeCodeJobStatus` devolvia o erro de stream cru, sem passar pelos detectores. Com isso, o job assíncrono nunca emitia os códigos canônicos que a documentação prometia. Corrigido: o watcher reconhece o evento final com `is_error=true` (via `Get-ClaudeCodeStreamEventErrorText`), o erro de stream passa pelos mesmos detectores do síncrono e `error_max_turns` casa o detector de esgotamento de turno.

Gap encadeado, achado na passada seguinte da mesma pré-push: com o watcher enxergando o desfecho, a prioridade de `FinalText` em `Resolve-ClaudeCodeJobStatus` passaria a **descartar** essa falha — e o esgotamento de turno **pode** chegar *depois* de algum texto, caso em que a resposta sairia como `completed` com parecer truncado, contando para o piso de diversidade do painel. Correção mínima: `failureAfterText` no shape do status e no `result.json`, preservando a evidência sem mudar a semântica de `status` para quem já consome. A decisão sobre parecer truncado continua sendo a reclassificação pós-hoc do `15`.

**Achados derivados, tratados em frentes próprias na mesma sessão:** o contrato de `-Tools ""`, que omitia a flag em vez de desabilitar ferramentas (invertendo o efeito documentado); e o parâmetro `-MaxTurns`, descartado em silêncio porque a CLI 2.1.215 deixou de anunciar `--max-turns` no `--help` — removido, com a direção de calibragem registrada em `998-ideias-descartadas-e-porque.md`.

### Rastreabilidade

- Arquivos materiais: `scripts/ClaudeCodeCliSupport.ps1`, `scripts/Invoke-ClaudeCode.ps1`, `scripts/Start-ClaudeCodeJob.ps1`, `scripts/Watch-ClaudeCodeJob.ps1`, `scripts/Test-ClaudeCodeCliSupportSelfTest.ps1`, `scripts/Test-InvokeLlmDelegatePanelDispatchSelfTest.ps1`, `xpz-llm-delegate/SKILL.md`, `09-inventario-e-rastreabilidade-publica.md`, `CHANGELOG.md`, `998-ideias-descartadas-e-porque.md`, `999-ideias-pendentes.md` (esta entrada removida).
- Evidência: `claude 2.1.215`; ensaios controlados em pastas descartáveis, com e sem `.claude/settings.json`, com e sem `--max-turns` explícito. Do achado assíncrono: job real em `claude 2.1.220`, cujo stream JSONL termina em `type=result`/`subtype=success` e cujo evento `init` traz `"tools":[]` sob `--tools ""` (confirmação de que o valor vazio desabilita as ferramentas de fato).
- Medição de fechamento (2026-07-25, `claude 2.1.220`, `--max-turns 1` explícito com `--tools Read`): o evento final é `{"type":"result","subtype":"error_max_turns","is_error":true,"terminal_reason":"max_turns","errors":["Reached maximum number of turns (1)"]}` — **sem** campo `result`. Dois ensaios independentes divergiram quanto a haver texto antes do corte (um gastou o turno só em `tool_use`; outro emitiu uma frase antes da ferramenta), o que derrubou a afirmação absoluta «chega sempre depois de texto», corrigida no código e nos quatro documentos onde ela havia sido escrita.
- Resíduo simétrico do mesmo absoluto, fechado na rodada seguinte: a **mensagem canônica** de `max-turns-exhausted` dizia «gastou todos os turnos antes de produzir resposta» — e é reusada justamente no caminho `failureAfterText`, onde houve texto. Reescrita para admitir texto parcial. Na mesma passada, o detector ganhou o gatilho `Reached maximum number of turns (N)`: a mensagem real de `errors[]` **não** casava `reached max turns`, e a detecção dependia só de o subtype vir prefixado na evidência montada.
- Cobertura fechada na varredura semântica do intervalo completo (2026-07-26): até então **nenhum** self-test executava `Watch-ClaudeCodeJob.ps1` — as funções da biblioteca eram cobertas isoladamente, mas a ligação entre elas e o `<GUID>.result.json` não tinha rede, justo onde o comportamento novo aparece. O self-test passou a dirigir o watcher de ponta a ponta contra stream sintético (texto parcial + evento real de `error_max_turns`, com o processo dono já morto), afirmando `status=completed`, `error` nulo e `failureAfterText` com a mensagem de `errors[]`.
- Self-test: `OK: Test-ClaudeCodeCliSupportSelfTest.ps1` (66 casos).
